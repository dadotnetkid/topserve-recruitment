@model IEnumerable<RecruitmentSystem.Recruitment.Data.sp_skills_listResult>
@{
    Layout = null;
}
<table class="table table-small-font table-hover table-striped ">
    <thead>
        <tr>
            <th>Skill</th>
            <th>Date Created</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var i in Model)
        {
            <tr>
                <td>@i.skill_name</td>
                <td>@i.date_created</td>
            </tr>

        }

    </tbody>
</table>
<div id="add-modal" class="modal fade" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <form action="@Url.Action("skills")" method="post" id="form-add">
                <div class="modal-header modal-header-danger">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title">Skills</h4>
                </div>
                <div class="modal-body">
                    <input type="text" id="skill" required="" class="form-control" name="skill" />
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    <button type="button" onclick="addform('@Url.Action("skills")','#form-add','@Url.Action("skills")','#skills','#alert-container','#skill')" class="btn btn-danger">Save changes</button>
                </div>
            </form>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div>
<div id="import-modal" class="modal fade" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <form id="import-form" method="post" action="@Url.Action("importskills")" enctype="multipart/form-data">
                <div class="modal-header modal-header-danger">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title">Import Skills</h4>
                </div>
                <div class="modal-body">
                    <input type="file" name="file" class="file-hidden" id="file" onchange="onchangeimportfilebrowse(this,'#filename')" />
                    <input type="text" class="form-control  " id="filename" placeholder="Filename" onclick="clickfilebrowse('#file')" />
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    <button type="button" onclick="FormSubmit('#import-form')" class="btn btn-danger">Import</button>
                </div>
            </form>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div>